import styled from "@emotion/styled"

export const WrapAnimeList = styled.div`
  position: relative;

  .card-list {
    position: relative;
    display: flex;
    justify-content: flex-start;
    flex-wrap: wrap;
    margin: 0 auto;
    left: 7px;
    right: 0;
    bottom: 0;
  }
`